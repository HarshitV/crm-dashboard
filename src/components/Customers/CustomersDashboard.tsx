import { rem } from "@/utils/rem";
import { Box } from "@chakra-ui/react";

export const CustomersDashboard = () => {
  return (
    <Box bg="white" borderRadius={rem(30)} h={rem(150)}>
      Customers Dashboard - To be implemented
    </Box>
  );
};
